# Findings that are not secrets.
#
# gitleaks scans the whole history, so a value that has been rewritten is
# still found in the commit that introduced it. Rewriting published history
# to hide a non-secret would be worse than the finding.
#
# Every line here must be justified. If you cannot explain in one sentence
# why a value is harmless, it is not harmless: rotate it instead.

# A test fixture: a placeholder BETTER_AUTH_SECRET that only ever existed to
# satisfy the env schema's minimum length in tests/auth/signup-gate.test.ts.
# It was never a real secret, never deployed, and has since been replaced
# with a self-describing low-entropy string.
21153f8c8258c0d06f61c5f6a4271d43b0629d4d:tests/auth/signup-gate.test.ts:generic-api-key:23
